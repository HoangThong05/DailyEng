# Ảnh cho cửa hàng (tuỳ chọn)

- Bìa: `<key>.webp`, 1200×400, ví dụ `bia-trung-thu.webp`; rồi đặt `art: true` cho vật phẩm đó trong `lib/shop.ts`.
- Không có file thì app vẽ cảnh bằng code (scene trong lib/shop.ts).
